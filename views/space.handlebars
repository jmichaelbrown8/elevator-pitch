<div class='container'>
  <div class='row justify-content-between align-items-center'>
    <div class="col-6">
        <h3>{{space.name}}</h3>
    </div>
    <div class='col-6'>
        <div class='card card-body'>
            Add a new idea to {{space.name}}
            <a class='btn btn-primary' href='/space/space_id/idea'>Add Idea</a>
        </div>
    </div>
  </div>
</div>
{{#each space.ideas}}
  {{> ideaMini }}
{{/each}}

{{! Create a space form using the space name I entered on home page }}

